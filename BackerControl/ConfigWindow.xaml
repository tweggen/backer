<Window x:Class="BackerControl.ConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:BackerControl"
        mc:Ignorable="d"
        Title="Configuration"
        Height="250" Width="400"
        WindowStartupLocation="CenterScreen"
        >
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Cloud Service URL -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Cloud Service URL:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="CloudUrlTextBox" Margin="5"/>

        <!-- Email -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="EmailTextBox" Margin="5"/>

        <!-- Password -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Password:" VerticalAlignment="Center"/>
        <PasswordBox Grid.Row="2" Grid.Column="1" Name="PasswordBox" Margin="5"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Save" Width="80" Margin="5" Click="OnSaveClicked"/>
            <Button Content="Cancel" Width="80" Margin="5" Click="OnCancelClicked"/>
        </StackPanel>
            
    </Grid>
</Window>